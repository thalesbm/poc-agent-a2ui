
**How to setup the project**
```bash
python -m venv .venv
source .venv/bin/activate 
pip install -r agent/requirements.txt
```

To run local execute:
python agent/local_main.py


cd frontend
npm install
npm start